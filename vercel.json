{
    "devCommand": "uvicorn ingest:app --host 0.0.0.0 --port 3000 --reload",
    "builds": [
        {
            "src":"ingest.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {"src":"/(.*)",
        "dest": "main.py"
    
        }
    ]
}